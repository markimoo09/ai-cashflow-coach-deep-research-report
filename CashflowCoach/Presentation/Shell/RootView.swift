import SwiftUI

/// Root navigation container — placeholder until feature shells are built.
struct RootView: View {
    var body: some View {
        NavigationStack {
            ContentView()
                .navigationTitle("CashflowCoach")
        }
    }
}

#Preview {
    RootView()
}
